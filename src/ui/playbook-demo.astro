---
import type { PlaybookEntry } from "../data/playbook.ts";
import { PlaybookDemoRouter } from "./playbook/demo-router.tsx";

type Props = {
  entry: PlaybookEntry;
};

const { entry } = Astro.props as Props;
---

<PlaybookDemoRouter client:load slug={entry.slug} />
